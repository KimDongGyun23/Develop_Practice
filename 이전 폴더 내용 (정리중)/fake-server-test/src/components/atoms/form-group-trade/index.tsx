import styled from "styled-components";

const FormGroupTrade = styled.div`
  margin-bottom: 30px;

  & > h4 {
    padding: 10px 0;
    font-weight: 700;

    & > span {
      font-size: 0.9em;
      color: var(--color-grey-500);
    }
  }
`;

export default FormGroupTrade;
